/**
 * API Service using configured axios instance
 */
import axiosInstance from '../config/axiosConfig';

// Export the configured axios instance as apiClient
const apiClient = axiosInstance;

export default apiClient;

